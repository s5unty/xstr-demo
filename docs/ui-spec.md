# 输入 UI 说明

- UI 实现方式：DOM Overlay（不嵌入 Three.js）。
- 区域：
  - 目标文本行：已输入绿色、待输入灰色、当前位置光标。
  - 码表行：显示当前字母缓冲。
  - 候选行：显示 `1~4` 候选编号。
  - 状态行：操作提示与流程提示。
  - 指标行：CPM/正确率/连击。
- 焦点：点击画布后仍以窗口键盘输入为主。
- TODO: 候选选中态与页码动画未实现，后续按视觉稿补充。
