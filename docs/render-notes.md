# 渲染说明

- 场景：单相机 + 半球光 + 方向光。
- 键位图：Three.js 3D 键帽 + 图例平面（混合方案），键位布局与原 `1078x412` 资源坐标对齐。
- 交互控制：右键旋转、中键平移、滚轮缩放；左键点击键帽触发真实输入。
- 键帽反馈：按键时执行按压 + 亮度提升 + 冒泡回弹动画，回弹时长由击打间隔驱动。
- 反馈：
  - 正确：背景闪为深绿
  - 错误：背景闪为深红
- 性能：键帽网格、材质、贴图在初始化时创建；Raycast 仅在 pointer 事件触发，不在每帧执行。
