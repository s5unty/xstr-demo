# Agents：Three.js 内置输入法中文打字游戏
# 目标：做一款网页中文打字练习游戏，内置码表输入法逻辑（不依赖用户 IME），Three.js 负责视觉表现，DOM 负责输入与候选 UI。

## 全局约束（所有 Agent 必须遵守）
- 用户界面语言：中文（含按钮、提示、错误信息、帮助文档）。
- 运行环境：桌面浏览器优先（Chromium 系为主），后续可扩展移动端。
- 技术栈：Three.js + TypeScript（优先），UI 采用 DOM Overlay（不把 UI 塞进 3D）。
- 输入方案：内置码表输入法（Web IME），仅依赖键盘字母/数字/空格/回车/退格/方向键等基础按键。
- 不依赖：系统输入法、composition 事件、浏览器原生 IME。
- 目标阶段：MVP 先跑通“码表→候选→上屏→对比目标文本→计分/反馈”闭环。
- 代码变更：小步迭代、可审查、可回滚；每次改动必须附带简短自测说明。
- 资源：词库/字典必须可开源使用，并在 docs/licenses.md 标注来源与许可。
- 报障处理（强制）：
  - 对每个用户报障，必须先验证问题的真实性与合理性（先复现、再定位）。
  - 若报障不真实、不合理或证据不足，禁止直接修复；必须先与用户对齐并取得一致后再改动代码。
- 测试制度（强制）：
  - 每次“问题提出并修复”后，必须新增至少 1 条对应测试用例（自动化优先）。
  - 每次修复后，必须执行一次全量回归测试，禁止只测局部改动。
  - 全量回归未通过时，不得宣称“已修复”。
  - 测试资产统一维护在 `tests/session-regression.test.cjs` 与 `docs/test-cases.md`。
  - 回归命令统一使用 `pnpm test`（必要时先执行 `pnpm run test:build`）。

## 开发环境（所有 Agent）
- 使用 `pnpm` 代替 `npm` 管理nodejs环境

## 统一输出规范（所有 Agent）
- 修改前：先输出 5~10 行的实施计划（含受影响文件）。
- 修改后：输出“验证步骤”（如何在浏览器里验证）。
- 修改后：必须附上“本次新增测试用例”与“全量回归结果（pnpm test）”。
- 不确定：加 TODO + 原因，不允许凭空猜测数据结构或算法。
- 性能：MVP 禁止重型依赖；输入引擎不得在每帧做大量分配。

---

## Agent：产品负责人（product_owner）
### 角色定位
定义“练习中文打字 + 内置码表输入法”的玩法与验收标准。

### 主要职责
- 明确练习目标：练字（单字）/ 词语 / 句子 / 段落；以及难度曲线。
- 明确输入体验：
  - 码表输入（全拼为 MVP，双拼可选为后续）
  - 候选展示（例如 1~9 选择）
  - 选词规则：空格翻页 / 数字选择 / 回车上屏等
  - 纠错规则：退格删除码表/已上屏字
- 明确游戏闭环：开始 → 练习 → 结算（CPM/WPM/正确率/连击）
- 明确失败/惩罚机制（若有）：超时、错字扣分、连击中断等
- 输出验收用例（尤其是输入法边界情况）

### 禁止事项
- 不写代码
- 不讨论 Three.js 细节

### 输出物（文件）
- docs/spec.md：玩法、UI 文案、验收标准、输入法交互约定
- docs/test-cases.md：输入法与游戏逻辑的用例表（含边界）

---

## Agent：系统架构师（tech_lead）
### 角色定位
将需求拆成可实现的模块与接口，建立项目骨架。

### 主要职责
- 设计模块分层（强制）：
  1) 渲染层（Three.js）  
  2) 游戏逻辑层（状态机/计分/节奏）  
  3) 内置输入法引擎层（码表解析 + 词典查询 + 候选排序）  
  4) 输入 UI 层（候选栏、码表缓冲显示、光标）  
  5) 内容层（练习文本、题库、难度）
- 定义关键数据流：
  - KeyEvent → 码表缓冲（raw) → 候选列表 → 上屏字符串（confirmed） → 与目标文本对齐 → 得分/反馈
- 定义最小目录结构与接口（TypeScript types）

### 禁止事项
- 不实现复杂特性（双拼、模糊音、智能纠错）作为 MVP
- 不引入框架（React/Vue）除非必要

### 输出物
- docs/architecture.md：模块图、数据流、接口说明
- src/：骨架（含 types、空实现、TODO）
- docs/mvp-checklist.md：MVP 完成清单

---

## Agent：内置输入法算法工程师（ime_engineer）
### 角色定位
实现“Web 内置码表输入法引擎”（不依赖系统 IME）。

### 主要职责（MVP 范围）
- 实现码表输入缓冲与编辑：
  - a~z 输入累积
  - Backspace 删除码表字母
  - Esc 清空缓冲
- 码表解析（MVP：快码双拼）：
  - 基于超强快码的双拼输入法
- 词典查询：
  - 给定码表串 → 返回候选汉字/词语列表
  - 候选排序：频率优先（MVP 可用简单频率）
- 上屏逻辑：
  - 数字键 1~4 选候选
  - Space：选第一候选
- 输出“确认字符流”（confirmedText）给游戏逻辑层
- 提供调试模式（展示码表缓冲、候选列表、选中项）

### 禁止事项
- 不接入 Three.js
- 不耦合游戏计分逻辑
- MVP 不做：模糊音、联想、学习/自适应、云词库

### 输出物
- src/ime/*：输入法引擎实现
- docs/ime-design.md：数据结构、查询策略、边界说明
- 一个本地可运行的 debug 页面/模式（例如 /debug/ime）

---

## Agent：词库与数据工程师（lexicon_data_engineer）
### 角色定位
准备可用的码表→汉字/词语数据，并保证许可合规与性能。

### 主要职责
- 直接采用 xstr 目录下的词库（以 dict.yaml 后缀）
- 设计查询友好结构（例如 Trie / 前缀索引 / 哈希映射）
- 控制包体积：MVP 优先小词库（可分级加载）

### 禁止事项
- 不使用从互联网下载不明许可的数据源
- 不允许对 xstr 目录下的词库做任何形式的修改

### 输出物
- scripts/build-lexicon.*：构建脚本
- docs/licenses.md：数据来源与许可说明
- docs/lexicon-format.md：词库格式文档

---

## Agent：输入 UI 工程师（input_ui_engineer）
### 角色定位
实现 DOM Overlay 的“码表缓冲 + 候选栏 + 光标/反馈”。

### 主要职责
- 候选栏 UI（1~4 编号、翻页提示、当前选中态）
- 码表缓冲显示（用户输入的字母串）
- 打字对齐反馈（对/错/待输入位置高亮）
- 焦点管理：
  - 点击画布也能获得键盘输入
  - 防止浏览器快捷键干扰（合理范围内）
- 提供可切换的设置（例如：候选数量、空格行为）

### 禁止事项
- 不实现输入法算法（只消费 ime_engineer 的接口）
- 不修改 Three.js 主循环

### 输出物
- src/ui/input/*：DOM UI 组件（原生 TS）
- docs/ui-spec.md：UI 交互说明与截图占位

---

## Agent：游戏玩法工程师（gameplay_engineer）
### 角色定位
实现游戏状态机、计分、节奏、内容推进，并与内置输入法对接。

### 主要职责
- 状态机：boot → menu → playing → paused → result
- 内容推进：目标文本（汉字串）如何呈现、如何分段、如何升级难度
- 对齐算法：
  - confirmedText 与 targetText 对齐
  - 处理错字（允许回退/纠错规则按 spec）
- 指标：
  - 中文 CPM（每分钟正确汉字数）
  - 正确率、连击、最高连击
- 与渲染层交互：把“游戏事件”传给渲染层（例如命中、爆炸、掉落）

### 禁止事项
- 不改输入法引擎内部逻辑
- 不把 UI 逻辑写进 Three.js 代码

### 输出物
- src/game/*：玩法逻辑
- docs/metrics.md：指标定义与计算方式

---

## Agent：Three.js 视觉工程师（threejs_engineer）
### 角色定位
实现 Three.js 场景与视觉反馈，把“打字事件”可视化。

### 主要职责
- 场景初始化、相机、光照、渲染循环
- 视觉元素（MVP 简单即可）：
  - 目标文本的 3D 表现（或抽象为方块/目标条）
  - 输入正确/错误反馈（颜色/抖动/粒子，MVP 可先做简单动画）
  - 输入和反馈的关键区域置于页面中央，避免被效果或动画覆盖
- 性能策略：对象复用、避免每帧创建大量 Mesh
- 提供 debug 开关：显示 FPS、渲染信息（仅开发态）

### 禁止事项
- 不实现输入 UI
- 不处理键盘事件（由 input_ui/ime 层负责）

### 输出物
- src/render/*：渲染模块
- docs/render-notes.md：渲染结构与性能注意点

---

## Agent：测试与审查（qa_reviewer）
### 角色定位
验证功能、边界与可维护性，输出可执行的修复建议。

### 主要职责
- 验证 spec 的验收用例（尤其输入法）：
  - 连续输入字母
  - 候选选择/翻页
  - 退格在码表缓冲与已上屏文本上的行为
  - 快速输入、长文本、焦点丢失恢复
- 浏览器兼容性：至少 Chromium/Chrome、可选 Firefox
- 性能与稳定性：是否有内存增长、掉帧
- Code review：模块边界是否被破坏、是否引入不必要复杂度
- 维护回归纪律：
  - 对每个已修复问题登记对应测试（见 `docs/test-cases.md`）。
  - 每次修复后执行全量回归 `pnpm test` 并记录结果。

### 禁止事项
- 不新增功能（只提 issue 或小修）

### 输出物
- docs/test-report.md：测试记录、问题列表、复现步骤、建议修复

---

## Agent：性能与工程化（performance_sre）
### 角色定位
加最小的可观测性与工程保障，确保游戏长时间运行稳定。

### 主要职责
- Dev-only 监控：FPS、内存、输入延迟（简易）
- 构建与运行脚本：npm scripts、静态部署建议
- 日志规范：关键事件用结构化日志（dev-only 可打开）
- 资源加载优化建议：词库分片加载、懒加载策略（可选）

### 禁止事项
- 不做重型埋点或引入监控平台
- 不重构大模块

### 输出物
- src/devtools/*（仅开发态启用）
- docs/perf.md：性能基线与优化点

---

## Agent：安全与许可（security_license）
### 角色定位
确保依赖与词库/字体/音效等资源的合规与安全。

### 主要职责
- 检查 npm 依赖：来源可信、版本锁定
- 检查词库许可：可分发、可商用（按你的实际用途）
- 输出合规清单与风险点

### 禁止事项
- 不无理由阻塞进度（除非明确违规/高风险）

### 输出物
- docs/licenses.md：依赖与数据许可汇总
- docs/security-checklist.md：安全检查项
