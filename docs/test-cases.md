# 输入法与玩法测试用例（会话回归）

## 自动化回归（Node）
文件：`tests/session-regression.test.cjs`  
命令：`pnpm test`

| ID | 场景 | 关键输入 | 预期 |
|---|---|---|---|
| IME-SESSION-01 | `;` 输入支持 | `;` | `raw` 正确累积 |
| IME-SESSION-02 | 码表纠正（好） | `hb` | 第一候选 `好` |
| IME-SESSION-03 | 连续词组识别 | `nhhbsmjd` | 候选包含 `你好世界` |
| IME-SESSION-04 | 长串 + 大写补码 | `crxusijixlymcuxclcxxhrGftke` | 包含 `程序设计需要持续练习和反馈` |
| IME-SESSION-05 | 长串无大写补码 | `crxusijixlymcuxclcxxhrftke` | 包含 `程序设计需要持续练习和反馈` |
| IME-SESSION-06 | 词频排序修正 | `xulu` | 第一候选 `效率` |
| IME-SESSION-07 | 潜在候选展示 | `xlh` | 含 `颉` 与 `需(~d)` |
| IME-SESSION-08 | 同字去重 | `rhb` | `仍` 不重复出现 |
| IME-SESSION-09 | 连续输入不中断 | `yczistuz` | 包含 `验证所有` |
| IME-SESSION-10 | `;`/`z` 一简片段 | `srchd;` / `srchdz` | 包含 `生成的` |
| IME-SESSION-11 | 标点映射 | `, . < > ?` | 上屏 `，。；！？` |
| IME-SESSION-12 | 输入中标点提交 | `nh` + `,` | 上屏 `你，` |
| IME-SESSION-13 | 多字词补码禁用 | `zrnxt` | 多字词无 `(~xx)` |
| IME-SESSION-14 | 左到右最长匹配回退 | `zrnxth` | 候选非空 |
| IME-SESSION-15 | 多音字变体 | `zrym` | 包含 `重要` |
| IME-SESSION-16 | 二码分段词组 | `sudjsohf` | 第一候选 `手动审核` |
| IME-SESSION-17 | 词频候选纠偏 | `wrmh` | 第一候选 `我们` |
| IME-SESSION-18 | 一简/U简前导 | `u` | 包含 `有(~;)` 或 `有(~z)` |
| IME-SESSION-19 | 直命中词优先 | `wdrc` | 第一候选 `围绕` |
| IME-SESSION-20 | 单独分号标点直出 | `;` | 上屏 `、` 且 `raw` 为空 |
| IME-SESSION-21 | 大写补码连续组词 | `bhshR` | 候选包含 `不伤` |
| IME-SESSION-22 | `ch` 候选排序修正 | `ch` | 第一候选 `从`，并含 `传(~j)`、`厂(~h)` |
| IME-SESSION-23 | 中英模式切换与英文直出 | `Shift` 后输入 `a,.` | 英文模式下 `a` `,` `.` 半角直出，不走中文标点映射 |
| IME-SESSION-24 | Shift 组合键不切模式 | `Shift` + `A` | 仍保持中文模式，仅输入大写 `A`，不触发模式切换 |
| IME-SESSION-25 | 中文模式数字直出 | 中文模式下输入 `0123456789` | `raw` 为空时，数字 `0-9` 直接半角上屏 |
| IME-SESSION-31 | 切图背景策略 | 读取键位图资源策略常量 | 黄字说明图使用透明黄字策略；按键图保持原图背景 |
| IME-SESSION-32 | 黄字说明图配置完整性 | 读取黄字说明图配置列表 | 包含左右分区与底部整图 `hints-bottom` |
| IME-SESSION-33 | 3D 键盘布局完整性 | 读取键位布局常量 | 共有 30 个键位，且包含 `a/semicolon/comma/period/slash` |
| IME-SESSION-34 | 3D 键盘鼠标交互语义 | 读取交互语义常量 | 左键按下下沉、松开回弹并在松开时提交输入；右键按住/拖拽旋转、右键单击退格；中键按住/拖拽平移、中键单击触发该键位大写输入；滚轮缩放 |
| IME-SESSION-35 | 3D 键盘背景策略 | 读取 3D 视觉策略常量 | 键盘底板背景关闭（`basePlate=none`），仅保留键帽与黄字说明 |
| IME-SESSION-36 | 3D 键帽贴图朝向 | 读取 3D 视觉策略常量 | 键帽贴图朝向外侧正面（`keyCapTextureFace=front`） |
| IME-SESSION-37 | 3D 垂直旋转策略 | 读取 3D 视觉策略常量 | 垂直旋转范围放开到接近全球面（`minPolarAngle<=0.01` 且 `maxPolarAngle>=3.1`） |
| IME-SESSION-38 | 3D 键帽结构策略 | 读取 3D 视觉策略常量 | 键帽使用单层渐变参数（`keyCapStructure/keyCapDepth/keyCapTopScale`） |
| IME-SESSION-39 | 3D 键帽颜色与初始视角 | 读取 3D 视觉策略常量 | 键帽侧面同色系浅色；初始倾角与相机高度满足立体视角策略 |
| IME-SESSION-40 | 3D 键盘初始上移策略 | 读取 3D 视觉策略常量 | 键盘初始 `Y` 偏移上移，避免与目标输入区域默认交叠 |
| IME-SESSION-41 | 3D 键盘空闲摇摆策略 | 读取 3D 视觉策略常量 | 空闲摇摆关闭，且左右/上下摇摆幅度参数为 0 |
| IME-SESSION-42 | 黄字说明与键盘整体上移策略 | 读取 3D 视觉策略常量 | 通过整体 `keyboardOffsetY` 上移保障安全距离；禁止单独上移 `hints-bottom` |
| IME-SESSION-43 | 黄字说明安全距离策略 | 读取 3D 视觉策略常量 | 键盘与黄字说明作为整体上移到足够高度，保证与目标区及其上方预留区有安全距离 |
| IME-SESSION-44 | 鼠标左键按键物理反馈策略 | 读取 3D 视觉策略常量 | 左键按下触发缩小与下沉；松开触发回弹上浮（非按下放大） |
| IME-SESSION-45 | 空闲摇摆禁用策略 | 读取 3D 视觉策略常量 | 键盘空闲摇摆关闭，且左右/上下摇摆幅度为 0 |
| IME-SESSION-46 | 构建类型声明覆盖 | 读取 `src/three.d.ts` | 声明文件覆盖 `three` 与 `OrbitControls` 模块，保障构建类型检查可通过 |

## 手工回归（UI/交互）

| ID | 场景 | 步骤 | 预期 |
|---|---|---|---|
| UI-01 | 目标文本居中与留白 | 启动后观察主界面 | 目标区居中，左右有安全间距 |
| UI-02 | 光标跟随与不漂移 | 连续输入 `sy` / `syy` / `syym` | 光标跟随推进，目标字不左右漂移 |
| UI-03 | 码表/候选锚定光标 | 输入任意编码 | 气泡出现在光标附近，左侧对齐 |
| UI-04 | 空输入隐藏候选 | 清空 `raw` | 候选气泡隐藏 |
| UI-05 | 光标动画 | 观察静止与移动 | 静止闪烁，移动有过渡与尾焰 |
| UI-06 | 头顶编码显示规则 | 观察普通字/一简字/标点 | 普通字最多2码；一简字1码；标点不显示编码 |
| UI-07 | 候选垂直展示 + 翻页键 | 输入多候选编码后按 `-`/`0` | 候选纵向4行，`-`下翻、`0`上翻 |
| UI-08 | 自定义目标粘贴 | 右下粘贴文本后点“应用目标” | 目标立即切换并提示成功 |
| UI-09 | 网页键位图与目标区防遮挡 | 启动后确认网页键位图可见，再粘贴超长目标文本 | 键位图始终可见；目标区有长度上限且不出现滚动条，不与键位图重叠 |
| UI-10 | 网页键位图规格显示 | 启动后观察网页键位图容器 | 桌面端按 `1078x412` 区域显示，按键布局完整，不被压缩变形 |
| UI-11 | 网页键位图居中且不被遮挡 | 启动后观察键位图位置，再粘贴大段目标文本并输入 | 键位图保持居中可见；目标区位于键位图下方独立区域，不覆盖键位图 |
| UI-22 | 网页键位图按键动画 | 开始游戏后连续按 `a`、`;`、`,`、`.`、`/` | 对应按键出现明显“放大上浮再回落”的冒泡动画（类似手机输入法）；回落为连续平滑缩放，不出现 3 段式停顿；松开后恢复；失焦后高亮自动清空 |
| UI-24 | 冒泡回落时长随击打速度变化 | 先快速连续敲击，再以明显慢速敲击同一按键 | 快速敲击时回落更快；慢速敲击时回落更慢，且过渡自然不突兀 |
| UI-23 | 网页键位图保留教学图例 | 启动后观察键位图文字说明与键帽内容 | 保留黄字分区（撇区/竖区/折区/点区/横区）；底部黄字提示以“单张完整图例”展示（不是多段拼接）；仅黄色说明文字图背景透明并只保留黄色内容；键帽保持原图风格与红色图例标记 |
| UI-12 | 单行主输入区与上下文分层 | 粘贴超长目标，连续输入推进光标 | 目标区仅中间 1 行显示“当前输入行”；上方显示已输入上下文，下方显示待输入上下文，视觉明显区分 |
| UI-13 | 上下文省略号提示 | 粘贴超长目标并输入到中段 | 若上方/下方仍有更多文本，对应方向显示 `…`，且目标区无滚动条 |
| UI-14 | 编码显示分层规则 | 连续输入并观察三层文本 | 中间主输入行不显示头顶编码；上/下文行显示头顶编码；三层文本宽度一致 |
| UI-15 | 光标边界与开局空白 | 打开页面观察后按 Enter 开始 | Enter 前目标区为空且光标不越界；Enter 后第一行文本进入目标区并可输入 |
| UI-16 | 上下文与主行同宽感知 | 连续输入并对比三层文本 | 上/下文字符字号与主行一致，不再出现明显“上下更窄”观感 |
| UI-17 | 开局下方待输入预览 | 进入菜单态（未按 Enter）观察目标区 | 中间主输入行为空，但下方显示“即将输入”的首行文本；若后续还有内容显示 `…` |
| UI-18 | 光标在主输入框内 | 开始游戏后连续输入并观察光标 | 光标始终位于中间主输入框内部，不再落到框体下方 |
| UI-19 | 输入模式提示与主行垂直居中 | 观察默认模式后按 `Shift` 来回切换，再输入 | 页面显示“输入模式：中文/英文（Shift切换）”；主输入行文本与光标位于输入框垂直中部 |
| UI-20 | 上下文区与背景融合 | 开始游戏后观察上下文显示区域 | 上下文在主输入框外显示；外层区域透明、无边框、无底色，与背景融合 |
| UI-21 | 主输入行右侧防越界 | 连续输入使当前行接近满行 | 当前输入文本不超出主输入框右边界；必要时自动减少单行显示字符并在框内裁剪 |
| UI-25 | 3D 键盘视角旋转 | 启动后按住鼠标右键拖动 | 键帽随视角变化，透视关系实时改变 |
| UI-26 | 3D 键盘平移与缩放 | 中键拖拽后滚轮缩放 | 视角可平移和缩放，键帽与图例始终保持同一键盘坐标系 |
| UI-27 | 鼠标点击键帽触发真实输入 | 开始游戏后左键点击 `a` / `;` / `,` | 输入法 `raw/候选/上屏` 与硬件键盘行为一致，并触发键帽按压反馈 |
| UI-28 | 3D 键盘垂直旋转 | 启动后按住右键向上/向下拖拽 | 视角可从近顶视角连续旋转到近底视角，键盘上下与背侧均可观察 |
| UI-29 | 3D 键帽立体增强 | 启动后在斜视角观察任意键帽 | 键帽厚度明显，不再扁平；单层键帽从上到下为平滑、圆润的外轮廓渐变 |
| UI-30 | 3D 键帽颜色一致性 | 启动后旋转视角观察键帽正面与侧面 | 正面与侧面均为同色系浅色观感，不再出现明显黑色侧边断层 |
| UI-31 | 3D 键盘初始立体角度 | 刷新页面观察初始视角 | 初始视角即呈现明显 3D 透视，不再像接近平铺的 2D 图 |
| UI-32 | 3D 键盘与目标区初始不重叠 | 刷新页面后不操作，观察键盘底边与目标输入区域 | 键盘底边与目标区域存在可见间距，不发生默认重叠 |
| UI-33 | 3D 键盘空闲翘翘板运动 | 刷新页面后停止鼠标操作约 2 秒，持续观察键盘 | 键盘出现细微且可察觉的左右/上下翘翘板式运动，并伴随轻微上下浮动；开始鼠标操作后运动减弱或暂停 |
| UI-34 | 黄字说明与目标区不重叠 | 刷新页面后观察底部黄字说明与目标输入区域 | 键盘与黄字说明保持整体结构并整体上移；黄字说明位于目标输入区域及其上方预留文本区上方，不出现初始交叠 |
| UI-35 | 初始视角立体增强 | 刷新页面后不操作直接观察键盘 | 键盘初始俯视倾角更明显，空间纵深感强于此前版本 |
| UI-36 | 左键按下/松开物理按键反馈 | 左键按住任意键帽约 1 秒后松开 | 按下时立即下沉；松开时回弹上浮；编码输入以松开时刻触发 |
| UI-38 | 右键点击退格与右键按住旋转分流 | 在有已上屏文本时先右键单击，再右键按住拖拽 | 右键单击触发一次退格删除；右键按住拖拽保持旋转视角，不触发退格 |
| UI-39 | 中键点击大写与中键按住平移分流 | 中键单击字母键帽（如 `a`），再中键按住拖拽 | 中键单击触发对应大写输入（如 `A`）；中键按住拖拽仅平移视角，不触发输入 |
| UI-37 | 空闲时键盘保持稳定 | 刷新页面后不做任何鼠标/键盘操作并观察 10 秒 | 键盘保持静止稳定，不再出现左右/上下摇摆 |
